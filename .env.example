# Redis broker URL — provision a Redis service on Railway and set this to its
# internal or public URL, e.g. redis://default:<password>@<host>:<port>/0
CELERY_BROKER_URL=redis://localhost:6379/0

# Redis result backend — can point to the same Redis instance as the broker
CELERY_RESULT_BACKEND=redis://localhost:6379/0
